{
  "name": "digital-backbone-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "coverage": "npm run test --workspaces -- run --coverage",
    "lint": "npm run lint --workspaces",
    "lint:semantic": "npm run start -w packages/semantic-linter -- audit"
  },
  "dependencies": {
    "@google/genai": "^1.31.0"
  }
}
